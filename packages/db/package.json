{
  "name": "@duora/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo No database tests yet"
  },
  "dependencies": { "@prisma/client": "^6.4.1" },
  "devDependencies": { "prisma": "^6.4.1", "typescript": "^5.7.3" }
}
